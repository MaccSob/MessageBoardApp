const { Client } = require('pg');
const fs = require('fs');

// Database connection configuration
const dbConfig = {
  user: 'locahost',
  host: 'locahost',
  database: 'messages',
  password: 'password',
  port: 5432, 
};

